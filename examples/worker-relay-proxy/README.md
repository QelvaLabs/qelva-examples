# Worker Relay Proxy (Planned)

Placeholder for an example demonstrating:

- Using an edge/worker environment as a thin proxy
- Forwarding intents from clients to a Qelva-core backend / relayer

This example is not implemented yet.
